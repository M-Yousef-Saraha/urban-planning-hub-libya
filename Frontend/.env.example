# Frontend environment example for Vite
# Copy this to Frontend/.env.local or Frontend/.env and adjust values.

# Base URL for API used by the frontend (include protocol and port)
VITE_API_BASE_URL=http://localhost:8000

# Example: analytics or feature flags
# VITE_ENABLE_ANALYTICS=false

# Notes:
# - Vite only exposes env variables prefixed with VITE_ to the client bundle.
# - After editing .env, restart the dev server (npm run dev) to pick up changes.
